import DiceGame from "./components/DiceGame"; // Match the import to the component being used

function App() {
  return (
    <div className="App">
      <DiceGame />
    </div>
  );
}

export default App;
